import classes from './global-footer.module.sass'

const GlobalFooter = () => {
  return (
    <footer className={`${classes.footer} footer`}>
      <div>footer</div>
    </footer>
  )
}

export default GlobalFooter
